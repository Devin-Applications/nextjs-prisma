export interface Todo {
  id: string;
  created: string;
  text: string;
  completed: boolean;
}

export interface Vendor {
  id: string;
  name: string;
  contact: string;
  services: string;
  createdAt: string;
}
